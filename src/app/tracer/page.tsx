import React from "react"

export default function TracerPage() {
  return (
    <section>
      <h2 className="text-2xl font-semibold">Tracer Agent</h2>
      <p className="text-sm text-slate-600">Tracer and skip-trace conversational UI (mock).</p>
    </section>
  )
}
