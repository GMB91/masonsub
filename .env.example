# Environment Variables for Type Maintenance

# ============================================================================
# Supabase Configuration
# ============================================================================

# Required: Your Supabase project ID
# Find in: Supabase Dashboard → Settings → General → Reference ID
SUPABASE_PROJECT_ID=your-project-id-here

# Optional: Supabase URL (for runtime client)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Optional: Supabase anon key (for runtime client)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# ============================================================================
# Type Maintenance Configuration
# ============================================================================

# Optional: Auto-commit changes to git after successful verification
# Values: "true" | "false"
# Default: "false" (manual commit required)
AUTO_COMMIT=false

# ============================================================================
# Anthropic API Configuration
# ============================================================================

# Optional: Anthropic API Key for AI/LLM features
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key-here

# ============================================================================
# CI/CD Secrets (GitHub Actions)
# ============================================================================

# Add these as GitHub Repository Secrets:
# Settings → Secrets and variables → Actions → New repository secret
#
# Required:
#   - SUPABASE_PROJECT_ID (same as above)
#
# Optional (for Slack/Discord notifications):
#   - SLACK_WEBHOOK (Slack incoming webhook URL)
#   - DISCORD_WEBHOOK (Discord webhook URL)

# ============================================================================
# Notes
# ============================================================================

# This file should be copied to .env.local and values filled in
# Never commit .env.local to git (it's in .gitignore)
# 
# For production deployments:
# - Add these variables to your hosting platform (Vercel, Railway, etc.)
# - Ensure SUPABASE_PROJECT_ID matches your production Supabase project
